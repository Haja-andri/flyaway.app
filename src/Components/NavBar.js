import React from 'react';

export default function NavBar (){
    return(
        <nav className="dark">
        <a href="/">Home</a>
        <a href="/">Host</a>
        <a href="/">Logout</a>
      </nav>
    );
}